@using Newtonsoft.Json
@using System.Text
@using Newtonsoft.Json.Linq
@page "/test"
<h3>Component</h3>

<input @bind="request"/>
<button @onclick="sendRequest">Send request</button>
<p>@response</p>

@code {
	public string request = "";
	public string response = "";

	public async Task sendRequest()
	{
		string endpoint = "https://api.openai.com/v1/chat/completions";

		var messages = new[]
		{
			new { role = "user", content = request}
		};

		var data = new
		{
			model = "gpt-4",
			messages = messages,
			temperature = 0.2
		};

		string jsonString = JsonConvert.SerializeObject(data);
		var content = new StringContent(jsonString, Encoding.UTF8, "application/json");
		HttpClient client = new HttpClient();

		client.DefaultRequestHeaders.Add("Authorization", "Bearer sk-proj-UaLaQgZ5KUtnnmhEhN5aT3BlbkFJyrqDrfGkncoQuSaJhAaZ");

		var responseDos = await client.PostAsync(endpoint, content);
		string responseContent = await responseDos.Content.ReadAsStringAsync();
		var jsonResponse = JObject.Parse(responseContent);

		response = jsonResponse["choices"][0]["message"]["content"].Value<string>();
		await InvokeAsync(StateHasChanged);

	}
}